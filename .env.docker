# ===========================================
# DESARROLLO LOCAL CON DOCKER 
# ===========================================
# Usar este archivo cuando corras PostgreSQL en Docker
# Copiar como .env antes de iniciar el servidor
# ===========================================

# Configuración del servidor
PORT=3000
NODE_ENV=development

# Base de datos LOCAL (Docker PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tic_laboratorios"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/tic_laboratorios"

# Autenticación
JWT_SECRET="TIC-SECRET-LOCAL-DEV"

# URLs locales
BACKEND_URL="http://localhost:3000"
VITE_BACKEND_URL="http://localhost:3000"

# Integraciones (opcional)
N8N_FEEDBACK_WEBHOOK_URL=""