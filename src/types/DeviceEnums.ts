export enum Mueble {
  MESA = "mesa",
  RACK = "rack",
  LIBRE = "libre",
}

export enum TipoDispositivo {
  WORKSTATION = "workstation",
  SERVER = "server",
  ROUTER = "router",
  SWITCH = "switch",
  OTRO = "otro",
}
