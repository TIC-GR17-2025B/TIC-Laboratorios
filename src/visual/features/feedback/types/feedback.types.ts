/**
 * Tipos compartidos del módulo de Retroalimentación de Estudiante.
 * Fuente única de verdad para FeedbackData en el frontend.
 */
export interface FeedbackData {
  analisis: string;
  fortaleza: string;
  area_mejora: string;
  consejo: string;
}
